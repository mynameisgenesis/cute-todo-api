type Task {
  id: ID!
  title: String!
  status: String!
  priority: String!
  createdAt: String!
}

type Query {
  tasks: [Task!]!
  task(id: ID!): Task
}

type Mutation {
  createTask(title: String!, priority: String!): Task!
  updateStatus(id: ID!, status: String!): Task!
}
